lr: 3e-4
weight_decay: 0.01
betas: [0.9, 0.95]
warmup_steps: 500
max_steps: 50000
grad_accum_steps: 1
grad_clip: 1.0

batch_size: 32

eval_every: 500
checkpoint_every: 2000

# Loss weights
behavior_loss_weight: 1.0
masked_spike_loss_weight: 0.1
masked_spike_fraction: 0.15
